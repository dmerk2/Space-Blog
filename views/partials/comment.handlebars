<div class="comments">
    {{#each this}}
    <section class="comment">
        <section class="comment">
            <div class="meta">
                {{user.username}} commented on {{format_date createdAt}}
            </div>
            <div class="text">
                {{comment_text}}
            </div>

        </section>
    </section>
    {{/each}}
</div>